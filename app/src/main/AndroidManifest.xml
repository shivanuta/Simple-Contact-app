<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mycontacts">

    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.WRITE_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyContacts">

        <activity
            android:name="com.mycontacts.activity.ContactListActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.mycontacts.activity.AddContactActivity"
            android:label="@string/add" />
        <activity
            android:name="com.mycontacts.activity.DeleteContactsActivity"
            android:label="@string/delete" />
        <activity
            android:name="com.mycontacts.activity.UpdateContactActivity"
            android:label="@string/update" />
    </application>
</manifest>